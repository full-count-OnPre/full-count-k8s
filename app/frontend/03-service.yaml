apiVersion: v1
kind: Service
metadata:
  name: full-count-front
  namespace: app
  labels:
    app.kubernetes.io/name: full-count-front
    app.kubernetes.io/part-of: full-count
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: full-count-front
  ports:
    - name: http
      port: 80
      targetPort: http
