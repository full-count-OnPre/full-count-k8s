apiVersion: v1
kind: Service
metadata:
  name: redis-master-service
  namespace: data
spec:
  selector:
    app: redis-master
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  type: ClusterIP
